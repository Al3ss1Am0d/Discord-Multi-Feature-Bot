# 🎙️ Text-to-Voice Bot

A **Text-to-Speech (TTS) Bot** that converts any message into audio.  
It works both in **chat** and in **voice chat (VC)**, allowing messages to be spoken in real time with synthesized voice.

---

## ✨ Features

- 📄 **Text-to-Speech in Chat**  
  - Captures messages sent in chat.  
  - Converts text into `.mp3` audio.  
  - Sends the audio file directly in the chat.  

- 🎧 **Voice Chat (VC) Integration**  
  - Automatically joins VC when called.  
  - Monitors chat in real time.  
  - Reads messages sent in chat aloud in VC.  

- 🔊 **Multiple Voice Options**  
  - Choose between different voice styles (male, female, robotic, etc).  
  - Easy configuration via commands.  

---

## 🚀 Installation

1. Clone this repository:
   ```bash
   git clone https://github.com/yourusername/text-to-voice-bot.git
   cd text-to-voice-bot
 
 2. Install dependencies:
    ```bash
    npm install

 3. Configure the .env file with your credentials:
    ```bash
    TOKEN=your_bot_token
    PREFIX=!